{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: local content plus snapshot autosync (fs writes to the sync folder, dialog to pick it) and the Schreib-Assistent's Ollama transport (http, exactly the local Ollama origin and nothing else).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "fs:allow-mkdir",
    "fs:allow-write-text-file",
    "fs:allow-exists",
    {
      "identifier": "fs:scope",
      "allow": [{ "path": "$APPDATA" }, { "path": "$APPDATA/**" }]
    },
    {
      "identifier": "http:default",
      "allow": [{ "url": "http://localhost:11434/*" }]
    }
  ]
}
