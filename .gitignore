# build output
dist/
.astro/
src-tauri/target/
src-tauri/gen/
# wrangler's local state: the miniflare D1/R2 emulation under `wrangler dev`.
.wrangler/

# dependencies
node_modules/

# environment
.env
.env.*
# Worker secrets for `wrangler dev` — the local stand-in for `wrangler secret put`.
.dev.vars
.dev.vars.*

# macOS
.DS_Store

# logs
npm-debug.log*

# The official Wortliste PDFs are copyrighted transcription sources — never committed.
docs/Goethe-Zertifikat_B1_Wortliste.pdf

# Official Goethe-Institut exam sets (PDFs, audio, video) and everything derived from
# them — sliced audio, rendered task pages, the local exam manifest — are copyrighted.
# The exam trainer (ADR 0009) reads them from these local paths; they never enter the repo.
docs/GeotheInstitute/
sources/goethe-exams/
public/exams/

# The editorial console's OUTPUT (`bun run redaktion`) — a generated instrument, regenerated
# on demand from content/ and data/, never committed.
#
# Anchored with a leading slash. A bare `redaktion/` matches a directory of that name at ANY
# depth, so when the console's source moved into `scripts/redaktion/` on 2026-08-14 this line
# silently swallowed the whole thing — eleven new modules, untracked, invisible in `git status`,
# and gone on the next clean checkout. The generated file is at the repo root; say so.
/redaktion/

# design-sync (claude.ai/design)
.ds-sync/
ds-bundle/
.design-sync/.cache/

# Local credentials — Cloudflare R2 today. Never committed; the deploy reads
# CLOUDFLARE_API_TOKEN / CLOUDFLARE_ACCOUNT_ID from repository secrets instead.
setenv.sh

# Large local ML checkpoints
.models/
.private/
.design-sync/learnings/
.design-sync/node_modules

# Listening Studio local Python state
tools/listening-studio/.venv/
tools/listening-studio/.mypy_cache/
tools/listening-studio/.pytest_cache/
tools/listening-studio/.ruff_cache/
tools/listening-studio/**/__pycache__/
feedback.md
# Screenshots attached to the feedback inbox — private, like feedback.md itself.
/IMG_*.jpeg
/IMG_*.png
