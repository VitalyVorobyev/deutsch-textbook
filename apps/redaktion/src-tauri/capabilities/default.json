{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Redaction frontend: core invoke plus a folder picker. Filesystem and shell stay behind Rust and the corpus sidecar.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
