# Listening generation brief — ls-freizeit-koennen-01

## Model-drafted; the submitted brief was not retained

This script was drafted by a language model, but the prompt that produced it was not saved and cannot be recovered. It is deliberately not reconstructed from the final payload: that string would describe the edited text, not the model's input.
