{
  "//": "Does this answer count? Pure, browser-safe, zero dependencies — the same decision has to be reachable from a React island grading a learner's typing, from `bun run validate` checking that a committed grading ruling still passes today's grader, and from progress:audit re-attributing an old attempt. It is the most load-bearing runtime contract in the repo: a change here moves the gate every recorded attempt was measured against, so it gets its own name rather than hiding inside the app.",
  "name": "@da/grading",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "exports": {
    "./cloze": "./src/cloze.ts",
    "./production": "./src/production.ts",
    "./worddiff": "./src/worddiff.ts"
  }
}
