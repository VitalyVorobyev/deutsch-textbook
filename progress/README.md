# Progress snapshots

JSON files exported from the site's **Fortschritt** page land here (named by date,
e.g. `2026-07-09.json`). They contain exercise attempts and FSRS card states.

The agent reads the newest snapshot to find weak spots and generate targeted drill
sets — see "Drills from progress" in [CLAUDE.md](../CLAUDE.md).
